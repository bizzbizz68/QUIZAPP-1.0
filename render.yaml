services:
  - type: web
    name: hsk-quiz-app
    env: static
    buildCommand: |
      flutter pub get
      flutter build web --release
    staticPublishPath: build/web
    envVars:
      - key: FLUTTER_VERSION
        value: "3.24.0"
